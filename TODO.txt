- Add "safe" arg to constructor that does first pass and checks whether alphabet
  contains all characters in data, UPDATE README when done
- Binary vs textual, UPDATE README when done
- Warning if too few entries are left in the initial table (slow...)
- Compute min_bits by default based on alphabet size

- Remember to clean code (delete $codes1, etc)
- Add Rake tests
- Package as gem

Readme:
- In Basic Usage, put correct output sample.

Test:
- Custom alphabets (same text using binary vs textual)
- Binary alphabets of weird sizes (very small, equal to power of 2, greater than 256, etc)
